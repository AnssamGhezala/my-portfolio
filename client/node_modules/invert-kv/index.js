'use strict';
module.exports = function (obj) {
	if (typeof obj !== 'object') {
		throw new TypeError('Expected an object');
	}

	var ret = {***REMOVED***

	for (var key in obj) {
		var val = obj[key];
		ret[val] = key;
	}

	return ret;
***REMOVED***
